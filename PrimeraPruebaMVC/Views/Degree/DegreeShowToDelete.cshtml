@model DegreeModel;
@{
    ViewData["Title"]="Borrar la Carrera";
}

    <div class="mb-3">
        ¿Esta seguro de que desea eliminar la carrera: @Model.Nombre?
    </div>

    <form asp-controller="Degree" asp-action="DegreeDelete">
        <button type="submit" class="btn btn-danger">Borrar</button>
        <a class="btn btn-secundary" asp-controller="Degree" asp-action="DegreeList">Regresar</a>
    </form>