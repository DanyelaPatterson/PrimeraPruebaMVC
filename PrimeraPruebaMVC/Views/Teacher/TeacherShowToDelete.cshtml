@model TeacherModel;
@{
    ViewData["Title"]="Borrar Maestro";
}

<div class="mb-3">
        ¿Esta seguro que desea eliminar el maestro: @Model.Nombre?
    </div>

<form asp-controller="Teacher" asp-action="TeacherDelete">

    <button type="submit" class="btn btn-danger">Confirmar</button>
    <a class="btn btn-secundary" asp-controller="Teacher" asp-action="TeacherList">Regresar</a>

</form>